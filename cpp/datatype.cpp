/*class decimal
{
    public:
    long numi;
    double numf;

    decimal()
    {
        numi=numf=0;
    }

    void num(double val)
    {
        if(val>=1)
        numi=(long)val;
        numf=val-numi;
    }

    void num(long val)
    {
        numi=val;
    }

    void print()
    {
        if(numi)
        cout<<numi;
        if(numi>0 && numf>0)
        cout<<"+";
        if(numf)
        cout<<numf;
    }

};
*/
