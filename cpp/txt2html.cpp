#include <iostream>
#include <fstream>
using namespace std;

int main()
{
    ifstream fin;
    ofstream fout;
    fin.open("C:/rahul/sqlite/2203outer12.txt", ios::in);
    fout.open("C:/rahul/sqlite/2203outer12.html", ios::out);

    fout<<"<!DOCTYPE html>\n<head>"
    while(!fin.eof())
    {

    }
}
